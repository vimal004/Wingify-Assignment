# Core - pinned as required by assignment
crewai==0.130.0
crewai-tools==0.47.1

# Web framework
fastapi>=0.110.0
uvicorn>=0.29.0
python-multipart>=0.0.9

# PDF processing
pypdf>=4.0.0

# Environment
python-dotenv>=1.0.0

# Database
sqlalchemy>=2.0.0
psycopg2-binary>=2.9.9  # Only needed for PostgreSQL (Neon/Render); not required for default SQLite

# Queue worker (optional — only needed for async endpoint)
celery>=5.3.0
redis>=5.0.0

# Testing
pytest>=8.0.0
httpx>=0.27.0  # For FastAPI TestClient async support

# Static files and compatibility
aiofiles>=23.2.1
google-auth>=2.28.0
google-generativeai>=0.4.0
